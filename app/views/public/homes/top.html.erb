<div class="container">
  <h1>ランダムレシピ</h1>
  <div class="row">
    <div class="col">
    <% @random.each do |recipe| %>
      <table class="table">
        <tr>
          <td>
            <%= link_to recipe_path(recipe.id) do %>
              <%= image_tag recipe.image, size: "100x100" %>
            <% end %>
          </td>
          <td><%= recipe.title %></td>
          <td><%= recipe.body %></td>
        </tr>
      </table>
    <% end %>
    </div>
  </div>
</div>